export interface Product {
    url?: string,
    name?: string,
    description?: string,
    image?: string,
    price: string,
    category: string,
    rating: string,
    inventory_status: string,
}